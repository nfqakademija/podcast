   <ul class="list-inline">
      {% for source in sources %}
      <li>
        <a href="{{ path('podcasts_by_source', {source: source.id}) }}" class="">{{ source.name}}</a>
      </li>
      {% endfor %}
    </ul>